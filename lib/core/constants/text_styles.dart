import 'package:flutter/material.dart';

class TextStyles {
  // For Navigation Drawer notifications
  static const unreadNotificationsForDrawer = TextStyle(
    fontWeight: FontWeight.bold,
    fontSize: 16,
  );

  // For read notifications
  static const readNotificationsForDrawer = TextStyle(
    fontWeight: FontWeight.normal,
    fontSize: 16,
  );

  static const headerText = TextStyle(
    fontWeight: FontWeight.w500,
    fontSize: 16,
    color: Colors.black87,
  );
}
